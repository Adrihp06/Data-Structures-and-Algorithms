'''Your input is an array of integers, and you have to reorder 
its entries so that the even entries appear first.'''

def even-odd(A):
  next-even, next-odd = 0, len(A) - 1
  while next-even < next_odd:
    if A[next-even] % 2 == 0:
      next_even += 1
    else :
      A[next-even], A[next_odd] =  A[next_odd], A[next-even]
      next_odd -= 1
  return A
  
  #time complexity O(n)
      
