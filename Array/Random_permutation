'''Design an algorithm that creates uniformly random permutations. You are given
a random number generator that returns integers with equal probability;
use as few calls to it as possible'''

def compute_random_permutation(n):
  permutation = list(range(n))
  random_sampling(n, permutation)
  return permutation
